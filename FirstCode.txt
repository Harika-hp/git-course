Hi Hello Harika!


//to initials=ze the git and specify the branch--> git init -b branchname
// to know the status of the file --> git status
//to add the file to the stagging area use --> git add filename
// to add all the files --> git add.
// to commit the changes --> git commit -m "Give the message"
// to get the logs of the changes ---> git log
// to perfome add and commit in same commad --> git commit -a -m "give message"
// to know what changes as been done---> git diff
// to delete the files --> git rm --cached filename

// to copy the code from github--> git clone mention the https path from github
// to connect the local files to git hub --> git remote add origin (mention the SSH link)
// to puch the code to github --> git push -u origin main  

// create a branch--> git switch -c (name of the branch)

// before merging pull the branch you need to merge --> git pull origin {branch name}
//merge the branch to main branch(make sure your in main barnch) --> git merge (Mention the branch name)
